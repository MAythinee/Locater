class FoodModel {
  // Field
  String nameFood,
      pathURL,
      nameShop,
      address,
      timeOpenClose,
      detail,
      lat,
      lng,
      postBy;

  // Constructor
  FoodModel(this.nameFood, this.pathURL, this.nameShop, this.address,
      this.timeOpenClose, this.detail, this.lat, this.lng, this.postBy);
}
